name: laundryin
description: "Aplikasi LaundryIN untuk UTS"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.8.4
  intl: ^0.19.0
  file_picker: ^8.1.1
  google_fonts: ^6.2.1
  package_info_plus: ^9.0.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
